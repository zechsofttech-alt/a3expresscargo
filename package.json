{
  "name": "a3-cargo-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  }
}
